# ArdaNova Unity SDK — Basic Setup

This sample demonstrates how to configure and use the ArdaNova SDK in a Unity project.

## Setup

1. Import the ArdaNova SDK package
2. Create an ArdaNovaSettings asset: Assets → Create → ArdaNova → Settings
3. Configure your API base URL and API key
4. See `SampleController.cs` for usage examples
