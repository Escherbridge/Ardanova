// Auto-generated from database-archietecture.dbml
// DO NOT EDIT DIRECTLY - modify the DBML file and run: npm run generate:prisma

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

generator zod {
  provider         = "zod-prisma-types"
  output           = "../src/lib/zod"
  useMultipleFiles = true
  createInputTypes = false
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  INDIVIDUAL
  GUILD
  ADMIN
}

enum UserType {
  INNOVATOR
  SUPPORTER
  VOLUNTEER
  FREELANCER
  SME_OWNER
  GUILD_MEMBER
}

enum UserTier {
  BRONZE
  SILVER
  GOLD
  PLATINUM
  DIAMOND
}

enum VerificationLevel {
  ANONYMOUS
  VERIFIED
  PRO
  EXPERT
}

enum NotificationType {
  TASK_ASSIGNED
  TASK_COMPLETED
  TASK_REVIEWED
  TASK_REVISION_REQUESTED
  PROPOSAL_CREATED
  PROPOSAL_VOTED
  PROPOSAL_PASSED
  PROPOSAL_REJECTED
  PROPOSAL_EXECUTED
  ACHIEVEMENT_EARNED
  LEVEL_UP
  STREAK_MILESTONE
  SHARES_RECEIVED
  SHARES_VESTED
  ESCROW_FUNDED
  ESCROW_RELEASED
  PROJECT_INVITATION
  PROJECT_INVITATION_ACCEPTED
  PROJECT_INVITATION_DECLINED
  PROJECT_MEMBERSHIP_REQUEST
  PROJECT_MEMBERSHIP_APPROVED
  PROJECT_MEMBERSHIP_REJECTED
  PROJECT_UPDATE
  PROJECT_FUNDED
  GUILD_INVITATION
  GUILD_INVITATION_ACCEPTED
  GUILD_INVITATION_DECLINED
  GUILD_APPLICATION
  GUILD_APPLICATION_APPROVED
  GUILD_APPLICATION_REJECTED
  EVENT_INVITATION
  EVENT_REMINDER
  EVENT_STARTING_SOON
  EVENT_CANCELLED
  EVENT_UPDATED
  USER_FOLLOWED
  PROJECT_FOLLOWED
  GUILD_FOLLOWED
  COMMENT_REPLY
  MENTION
  FOLLOWER_NEW
  SYSTEM_ANNOUNCEMENT
  SECURITY_ALERT
}

enum ActivityType {
  CREATED
  UPDATED
  DELETED
  COMPLETED
  JOINED
  LEFT
  COMMENTED
  VOTED
  SUBMITTED
  REVIEWED
  FUNDED
  TRANSFERRED
  SWAPPED
}

enum XPEventType {
  TASK_COMPLETED
  PROPOSAL_CREATED
  PROPOSAL_PASSED
  VOTE_CAST
  PROJECT_FUNDED
  MEMBER_REFERRED
  ACHIEVEMENT_EARNED
  STREAK_MAINTAINED
  LEVEL_UP
  REVIEW_GIVEN
  CONTRIBUTION_MADE
}

enum AchievementCategory {
  CONTRIBUTOR
  COLLABORATOR
  INVESTOR
  GOVERNANCE
  COMMUNITY
  STREAK
  MILESTONE
  GAMING
}

enum AchievementRarity {
  COMMON
  UNCOMMON
  RARE
  EPIC
  LEGENDARY
}

enum LeaderboardPeriod {
  DAILY
  WEEKLY
  MONTHLY
  QUARTERLY
  ALL_TIME
}

enum LeaderboardCategory {
  XP
  TASKS_COMPLETED
  SHARES_EARNED
  PROPOSALS_CREATED
  VOTES_CAST
  PROJECTS_FUNDED
}

enum StreakType {
  DAILY_LOGIN
  DAILY_CONTRIBUTION
  WEEKLY_TASK
}

enum ReferralStatus {
  PENDING
  COMPLETED
  EXPIRED
  CANCELLED
}

enum ProjectType {
  TEMPORARY
  LONG_TERM
  FOUNDATION
  BUSINESS
  PRODUCT
  OPEN_SOURCE
  COMMUNITY
}

enum ProjectDuration {
  ONE_TWO_WEEKS
  ONE_THREE_MONTHS
  THREE_SIX_MONTHS
  SIX_TWELVE_MONTHS
  ONE_TWO_YEARS
  TWO_PLUS_YEARS
  ONGOING
}

enum ProjectStatus {
  DRAFT
  PUBLISHED
  SEEKING_SUPPORT
  FUNDED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum ProjectCategory {
  TECHNOLOGY
  HEALTHCARE
  EDUCATION
  ENVIRONMENT
  SOCIAL_IMPACT
  BUSINESS
  ARTS_CULTURE
  AGRICULTURE
  FINANCE
  OTHER
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  REVIEW
  COMPLETED
  BLOCKED
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum TaskType {
  FEATURE
  BUG
  ENHANCEMENT
  DOCUMENTATION
  RESEARCH
  DESIGN
  TESTING
  REVIEW
  MAINTENANCE
  OTHER
}

enum SupportType {
  VOTE
  SUBSCRIPTION
  VOLUNTEER
  RESOURCE
}

enum ApplicationStatus {
  PENDING
  ACCEPTED
  REJECTED
  WITHDRAWN
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  DECLINED
  EXPIRED
  CANCELLED
}

enum MembershipRequestStatus {
  PENDING
  APPROVED
  REJECTED
  WITHDRAWN
}

enum RoadmapStatus {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED
}

enum PhaseStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum EpicStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum SprintStatus {
  PLANNED
  ACTIVE
  COMPLETED
  CANCELLED
}

enum PBIType {
  FEATURE
  ENHANCEMENT
  BUG
  TECHNICAL_DEBT
  SPIKE
}

enum PBIStatus {
  NEW
  READY
  IN_PROGRESS
  DONE
  CANCELLED
}

enum BacklogItemType {
  TASK
  BUG
  IMPROVEMENT
  RESEARCH
}

enum BacklogStatus {
  NEW
  READY
  IN_PROGRESS
  DONE
  BLOCKED
}

enum Priority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ProjectRole {
  FOUNDER
  LEADER
  CORE_CONTRIBUTOR
  CONTRIBUTOR
  OBSERVER
}

enum CompensationModel {
  FIXED_SHARES
  HOURLY_SHARES
  EQUITY_PERCENT
  HYBRID
  BOUNTY
  MILESTONE
}

enum SubmissionStatus {
  PENDING
  APPROVED
  REJECTED
  REVISION_REQUESTED
}

enum ProposalType {
  TREASURY
  GOVERNANCE
  STRATEGIC
  OPERATIONAL
  EMERGENCY
  CONSTITUTIONAL
  SHARES
}

enum ProposalStatus {
  DRAFT
  ACTIVE
  PASSED
  REJECTED
  EXECUTED
  CANCELLED
  EXPIRED
}

enum BidStatus {
  SUBMITTED
  UNDER_REVIEW
  ACCEPTED
  REJECTED
  WITHDRAWN
}

enum TaskBidStatus {
  SUBMITTED
  UNDER_REVIEW
  ACCEPTED
  REJECTED
  WITHDRAWN
  COMPLETED
}

enum GuildMemberRole {
  OWNER
  ADMIN
  MANAGER
  MEMBER
  APPRENTICE
}

enum ShopCategory {
  RETAIL
  SERVICES
  DIGITAL_PRODUCTS
  FOOD_BEVERAGE
  HEALTH_WELLNESS
  TECHNOLOGY
  FASHION
  HOME_GARDEN
  ARTS_CRAFTS
  EDUCATION
  OTHER
}

enum InvoiceStatus {
  DRAFT
  SENT
  PAID
  OVERDUE
  CANCELLED
}

enum PaymentMethod {
  BANK_TRANSFER
  USSD
  CARD
  WALLET
}

enum CampaignStatus {
  DRAFT
  SCHEDULED
  ACTIVE
  COMPLETED
  CANCELLED
}

enum EscrowStatus {
  NONE
  FUNDED
  RELEASED
  DISPUTED
  REFUNDED
}

enum TransactionType {
  DEPOSIT
  WITHDRAWAL
  TASK_PAYMENT
  PROPOSAL_EXECUTION
  DIVIDEND
  FEE
}

enum VestingFrequency {
  DAILY
  WEEKLY
  MONTHLY
  QUARTERLY
}

enum FundraisingStatus {
  PENDING
  ACTIVE
  SUCCESSFUL
  FAILED
  CANCELLED
  REFUNDING
}

enum ContributionStatus {
  PENDING
  CONFIRMED
  REFUNDED
  FAILED
}

enum WalletProvider {
  PERA
  DEFLY
  ALGOSIGNER
  WALLETCONNECT
  OTHER
}

enum SwapStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
}

enum PostType {
  POST
  PROJECT_UPDATE
  GUILD_ACTIVITY
  TASK_COMPLETED
  MILESTONE
  PROPOSAL
  SHOP_ITEM
}

enum PostVisibility {
  PUBLIC
  PROJECT_MEMBERS
  GUILD_MEMBERS
  PRIVATE
}

enum MessageStatus {
  SENT
  DELIVERED
  READ
  FAILED
  NOTSEEN
}

enum ConversationType {
  DIRECT
  GROUP
}

enum ConversationRole {
  MEMBER
  ADMIN
  OWNER
}

enum MimeType {
  IMAGE
  VIDEO
  AUDIO
  DOCUMENT
  ARCHIVE
  OTHER
}

enum EventStatus {
  DRAFT
  SCHEDULED
  LIVE
  COMPLETED
  CANCELLED
}

enum EventType {
  MEETING
  WORKSHOP
  WEBINAR
  TOWN_HALL
  CRITIQUE
  HACKATHON
  SOCIAL
  OTHER
}

enum EventVisibility {
  PUBLIC
  PROJECT_MEMBERS
  GUILD_MEMBERS
  INVITE_ONLY
}

enum AttendeeStatus {
  INVITED
  GOING
  MAYBE
  NOT_GOING
  ATTENDED
}

enum OpportunityType {
  GUILD_POSITION
  PROJECT_ROLE
  TASK_BOUNTY
  FREELANCE
  MENTORSHIP
  COLLABORATION
}

enum OpportunityStatus {
  DRAFT
  OPEN
  IN_REVIEW
  FILLED
  CLOSED
  CANCELLED
}

enum ExperienceLevel {
  ENTRY
  JUNIOR
  MID
  SENIOR
  LEAD
  EXPERT
}

model Account {
  id String @id @default(cuid())
  userId String
  type String
  provider String
  providerAccountId String
  refresh_token String? @db.Text
  access_token String? @db.Text
  expires_at Int?
  token_type String?
  scope String?
  id_token String? @db.Text
  session_state String?
  user User @relation("Account_userId", fields: [userId], references: [id])
}

model Session {
  id String @id @default(cuid())
  sessionToken String @unique
  userId String
  expires DateTime
  user User @relation("Session_userId", fields: [userId], references: [id])
}

model User {
  id String @id @default(cuid())
  email String @unique
  emailVerified DateTime?
  name String?
  image String?
  bio String? @db.Text
  location String?
  phone String?
  website String?
  linkedIn String?
  twitter String?
  role UserRole @default(INDIVIDUAL)
  userType UserType @default(INNOVATOR)
  isVerified Boolean @default(false)
  totalXP Int @default(0)
  level Int @default(1)
  tier UserTier @default(BRONZE)
  trustScore Decimal @default(0) @db.Decimal(18, 8)
  verificationLevel VerificationLevel @default(ANONYMOUS)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  accounts Account[] @relation("Account_userId")
  sessions Session[] @relation("Session_userId")
  userSkills UserSkill[] @relation("UserSkill_userId")
  userExperiences UserExperience[] @relation("UserExperience_userId")
  verificationTokens VerificationToken[] @relation("VerificationToken_identifier")
  notifications Notification[] @relation("Notification_userId")
  activities Activity[] @relation("Activity_userId")
  xPEvents XPEvent[] @relation("XPEvent_userId")
  userAchievements UserAchievement[] @relation("UserAchievement_userId")
  leaderboardEntries LeaderboardEntry[] @relation("LeaderboardEntry_userId")
  userStreaks UserStreak[] @relation("UserStreak_userId")
  referrals Referral[] @relation("Referral_referrerId")
  referrals1 Referral[] @relation("Referral_referredId")
  projects Project[] @relation("Project_createdById")
  projectTasks ProjectTask[] @relation("ProjectTask_assignedToId")
  projectMilestones ProjectMilestone[] @relation("ProjectMilestone_assigneeId")
  projectSupports ProjectSupport[] @relation("ProjectSupport_userId")
  projectApplications ProjectApplication[] @relation("ProjectApplication_userId")
  projectInvitations ProjectInvitation[] @relation("ProjectInvitation_invitedById")
  projectInvitations1 ProjectInvitation[] @relation("ProjectInvitation_invitedUserId")
  projectMembershipRequests ProjectMembershipRequest[] @relation("ProjectMembershipRequest_userId")
  projectMembershipRequests1 ProjectMembershipRequest[] @relation("ProjectMembershipRequest_reviewedById")
  projectComments ProjectComment[] @relation("ProjectComment_userId")
  projectUpdates ProjectUpdate[] @relation("ProjectUpdate_userId")
  roadmaps Roadmap[] @relation("Roadmap_assigneeId")
  roadmapPhases RoadmapPhase[] @relation("RoadmapPhase_assigneeId")
  epics Epic[] @relation("Epic_assigneeId")
  sprints Sprint[] @relation("Sprint_assigneeId")
  productBacklogItems ProductBacklogItem[] @relation("ProductBacklogItem_assigneeId")
  backlogItems BacklogItem[] @relation("BacklogItem_assigneeId")
  projectMembers ProjectMember[] @relation("ProjectMember_userId")
  taskSubmissions TaskSubmission[] @relation("TaskSubmission_submittedById")
  taskSubmissions1 TaskSubmission[] @relation("TaskSubmission_reviewedById")
  proposals Proposal[] @relation("Proposal_creatorId")
  votes Vote[] @relation("Vote_voterId")
  delegatedVotes DelegatedVote[] @relation("DelegatedVote_delegatorId")
  delegatedVotes1 DelegatedVote[] @relation("DelegatedVote_delegateeId")
  guilds Guild[] @relation("Guild_ownerId")
  guildMembers GuildMember[] @relation("GuildMember_userId")
  guildInvitations GuildInvitation[] @relation("GuildInvitation_invitedById")
  guildInvitations1 GuildInvitation[] @relation("GuildInvitation_invitedUserId")
  guildApplications GuildApplication[] @relation("GuildApplication_userId")
  guildApplications1 GuildApplication[] @relation("GuildApplication_reviewedById")
  projectBids ProjectBid[] @relation("ProjectBid_userId")
  guildReviews GuildReview[] @relation("GuildReview_userId")
  guildUpdates GuildUpdate[] @relation("GuildUpdate_createdById")
  shops Shop[] @relation("Shop_ownerId")
  products Product[] @relation("Product_userId")
  invoices Invoice[] @relation("Invoice_buyerId")
  invoices1 Invoice[] @relation("Invoice_userId")
  sales Sale[] @relation("Sale_buyerId")
  sales1 Sale[] @relation("Sale_userId")
  inventoryItems InventoryItem[] @relation("InventoryItem_userId")
  marketingCampaigns MarketingCampaign[] @relation("MarketingCampaign_userId")
  projectEquities ProjectEquity[] @relation("ProjectEquity_userId")
  shareHolders ShareHolder[] @relation("ShareHolder_userId")
  fundraisingContributions FundraisingContribution[] @relation("FundraisingContribution_userId")
  wallets Wallet[] @relation("Wallet_userId")
  taskEscrows TaskEscrow[] @relation("TaskEscrow_funderId")
  shareSwaps ShareSwap[] @relation("ShareSwap_userId")
  liquidityProviders LiquidityProvider[] @relation("LiquidityProvider_userId")
  posts Post[] @relation("Post_authorId")
  postLikes PostLike[] @relation("PostLike_userId")
  postComments PostComment[] @relation("PostComment_authorId")
  postBookmarks PostBookmark[] @relation("PostBookmark_userId")
  postShares PostShare[] @relation("PostShare_userId")
  conversations Conversation[] @relation("Conversation_createdById")
  conversationMembers ConversationMember[] @relation("ConversationMember_userId")
  chatMessages ChatMessage[] @relation("ChatMessage_userToId")
  chatMessages1 ChatMessage[] @relation("ChatMessage_userFromId")
  attachments Attachment[] @relation("Attachment_uploadedById")
  events Event[] @relation("Event_organizerId")
  eventAttendees EventAttendee[] @relation("EventAttendee_userId")
  eventCoHosts EventCoHost[] @relation("EventCoHost_userId")
  eventReminders EventReminder[] @relation("EventReminder_userId")
  userFollows UserFollow[] @relation("UserFollow_followerId")
  userFollows1 UserFollow[] @relation("UserFollow_followingId")
  projectFollows ProjectFollow[] @relation("ProjectFollow_userId")
  guildFollows GuildFollow[] @relation("GuildFollow_userId")
  opportunities Opportunity[] @relation("Opportunity_posterId")
  opportunityApplications OpportunityApplication[] @relation("OpportunityApplication_applicantId")
  opportunityUpdates OpportunityUpdate[] @relation("OpportunityUpdate_userId")
  opportunityComments OpportunityComment[] @relation("OpportunityComment_userId")
}

model UserSkill {
  id String @id @default(cuid())
  userId String
  skill String
  level Int @default(1)
  user User @relation("UserSkill_userId", fields: [userId], references: [id])
}

model UserExperience {
  id String @id @default(cuid())
  userId String
  title String
  company String
  description String? @db.Text
  startDate DateTime
  endDate DateTime?
  isCurrent Boolean @default(false)
  user User @relation("UserExperience_userId", fields: [userId], references: [id])
}

model VerificationToken {
  id String @id @default(cuid())
  identifier String
  token String @unique
  expires DateTime
  user User @relation("VerificationToken_identifier", fields: [identifier], references: [id])
}

model Notification {
  id String @id @default(cuid())
  userId String
  type NotificationType
  title String
  message String @db.Text
  data Json?
  isRead Boolean @default(false)
  readAt DateTime?
  actionUrl String?
  createdAt DateTime @default(now())
  user User @relation("Notification_userId", fields: [userId], references: [id])
}

model Activity {
  id String @id @default(cuid())
  userId String
  projectId String?
  type ActivityType
  entityType String
  entityId String
  action String
  metadata Json?
  createdAt DateTime @default(now())
  user User @relation("Activity_userId", fields: [userId], references: [id])
  project Project? @relation("Activity_projectId", fields: [projectId], references: [id])
}

model XPEvent {
  id String @id @default(cuid())
  userId String
  eventType XPEventType
  amount Int
  source String
  sourceId String?
  metadata Json?
  createdAt DateTime @default(now())
  user User @relation("XPEvent_userId", fields: [userId], references: [id])
}

model Achievement {
  id String @id @default(cuid())
  name String @unique
  description String
  category AchievementCategory
  criteria Json
  xpReward Int
  equityReward Decimal? @db.Decimal(18, 8)
  rarity AchievementRarity @default(COMMON)
  icon String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  userAchievements UserAchievement[] @relation("UserAchievement_achievementId")
}

model UserAchievement {
  id String @id @default(cuid())
  userId String
  achievementId String
  progress Int @default(0)
  earnedAt DateTime?
  user User @relation("UserAchievement_userId", fields: [userId], references: [id])
  achievement Achievement @relation("UserAchievement_achievementId", fields: [achievementId], references: [id])
}

model Leaderboard {
  id String @id @default(cuid())
  period LeaderboardPeriod
  category LeaderboardCategory
  startDate DateTime
  endDate DateTime
  createdAt DateTime @default(now())
  leaderboardEntries LeaderboardEntry[] @relation("LeaderboardEntry_leaderboardId")
}

model LeaderboardEntry {
  id String @id @default(cuid())
  leaderboardId String
  userId String
  rank Int
  score Int
  metadata Json?
  leaderboard Leaderboard @relation("LeaderboardEntry_leaderboardId", fields: [leaderboardId], references: [id])
  user User @relation("LeaderboardEntry_userId", fields: [userId], references: [id])
}

model UserStreak {
  id String @id @default(cuid())
  userId String @unique
  currentStreak Int @default(0)
  longestStreak Int @default(0)
  lastActivityDate DateTime?
  streakType StreakType @default(DAILY_LOGIN)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user User @relation("UserStreak_userId", fields: [userId], references: [id])
}

model Referral {
  id String @id @default(cuid())
  referrerId String
  referredId String @unique
  referralCode String?
  status ReferralStatus @default(PENDING)
  rewardClaimed Boolean @default(false)
  xpRewarded Int?
  equityRewarded Decimal? @db.Decimal(18, 8)
  createdAt DateTime @default(now())
  completedAt DateTime?
  referrer User @relation("Referral_referrerId", fields: [referrerId], references: [id])
  referred User @relation("Referral_referredId", fields: [referredId], references: [id])
}

model Project {
  id String @id @default(cuid())
  title String
  slug String @unique
  description String @db.Text
  problemStatement String @db.Text
  solution String @db.Text
  categories String @default("OTHER") @db.Text
  projectType ProjectType @default(TEMPORARY)
  duration ProjectDuration?
  status ProjectStatus @default(DRAFT)
  fundingGoal Decimal? @db.Decimal(18, 8)
  currentFunding Decimal @default(0) @db.Decimal(18, 8)
  supportersCount Int @default(0)
  votesCount Int @default(0)
  viewsCount Int @default(0)
  featured Boolean @default(false)
  isTrending Boolean @default(false)
  trendingScore Decimal @default(0) @db.Decimal(18, 8)
  trendingRank Int?
  trendingAt DateTime?
  tags String? @db.Text
  images String? @db.Text
  videos String? @db.Text
  documents String? @db.Text
  targetAudience String? @db.Text
  expectedImpact String? @db.Text
  timeline String? @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  publishedAt DateTime?
  fundedAt DateTime?
  completedAt DateTime?
  createdById String
  assignedGuildId String?
  activities Activity[] @relation("Activity_projectId")
  createdBy User @relation("Project_createdById", fields: [createdById], references: [id])
  assignedGuild Guild? @relation("Project_assignedGuildId", fields: [assignedGuildId], references: [id])
  projectTasks ProjectTask[] @relation("ProjectTask_projectId")
  projectResources ProjectResource[] @relation("ProjectResource_projectId")
  projectMilestones ProjectMilestone[] @relation("ProjectMilestone_projectId")
  projectSupports ProjectSupport[] @relation("ProjectSupport_projectId")
  projectApplications ProjectApplication[] @relation("ProjectApplication_projectId")
  projectInvitations ProjectInvitation[] @relation("ProjectInvitation_projectId")
  projectMembershipRequests ProjectMembershipRequest[] @relation("ProjectMembershipRequest_projectId")
  projectComments ProjectComment[] @relation("ProjectComment_projectId")
  projectUpdates ProjectUpdate[] @relation("ProjectUpdate_projectId")
  roadmaps Roadmap[] @relation("Roadmap_projectId")
  sprints Sprint[] @relation("Sprint_projectId")
  projectMembers ProjectMember[] @relation("ProjectMember_projectId")
  proposals Proposal[] @relation("Proposal_projectId")
  delegatedVotes DelegatedVote[] @relation("DelegatedVote_projectId")
  projectBids ProjectBid[] @relation("ProjectBid_projectId")
  projectShares ProjectShare[] @relation("ProjectShare_projectId")
  projectEquities ProjectEquity[] @relation("ProjectEquity_projectId")
  treasuries Treasury[] @relation("Treasury_projectId")
  posts Post[] @relation("Post_projectId")
  postShares PostShare[] @relation("PostShare_sharedToProjectId")
  events Event[] @relation("Event_projectId")
  projectFollows ProjectFollow[] @relation("ProjectFollow_projectId")
  opportunities Opportunity[] @relation("Opportunity_projectId")
}

model ProjectTask {
  id String @id @default(cuid())
  projectId String
  backlogItemId String?
  title String
  description String? @db.Text
  status TaskStatus @default(TODO)
  priority TaskPriority @default(MEDIUM)
  taskType TaskType @default(FEATURE)
  estimatedHours Int?
  actualHours Int?
  equityReward Decimal? @db.Decimal(18, 8)
  escrowStatus EscrowStatus @default(NONE)
  dueDate DateTime?
  completedAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assignedToId String?
  project Project @relation("ProjectTask_projectId", fields: [projectId], references: [id])
  assignedTo User? @relation("ProjectTask_assignedToId", fields: [assignedToId], references: [id])
  backlogItem BacklogItem? @relation("ProjectTask_backlogItemId", fields: [backlogItemId], references: [id])
  projectTaskDependencies ProjectTaskDependency[] @relation("ProjectTaskDependency_taskId")
  projectTaskDependencies1 ProjectTaskDependency[] @relation("ProjectTaskDependency_dependsOnId")
  sprintItems SprintItem[] @relation("SprintItem_taskId")
  taskCompensations TaskCompensation[] @relation("TaskCompensation_taskId")
  taskSubmissions TaskSubmission[] @relation("TaskSubmission_taskId")
  taskBids TaskBid[] @relation("TaskBid_taskId")
  taskEscrows TaskEscrow[] @relation("TaskEscrow_taskId")
  opportunities Opportunity[] @relation("Opportunity_taskId")
}

model ProjectTaskDependency {
  id String @id @default(cuid())
  taskId String
  dependsOnId String
  task ProjectTask @relation("ProjectTaskDependency_taskId", fields: [taskId], references: [id])
  dependsOn ProjectTask @relation("ProjectTaskDependency_dependsOnId", fields: [dependsOnId], references: [id])
}

model ProjectResource {
  id String @id @default(cuid())
  projectId String
  name String
  description String? @db.Text
  quantity Int @default(1)
  estimatedCost Decimal? @db.Decimal(18, 8)
  recurringCost Decimal? @db.Decimal(18, 8)
  recurringIntervalDays Int?
  isRequired Boolean @default(true)
  isObtained Boolean @default(false)
  createdAt DateTime @default(now())
  project Project @relation("ProjectResource_projectId", fields: [projectId], references: [id])
}

model ProjectMilestone {
  id String @id @default(cuid())
  projectId String
  title String
  description String? @db.Text
  targetDate DateTime
  completedAt DateTime?
  isCompleted Boolean @default(false)
  createdAt DateTime @default(now())
  assigneeId String?
  project Project @relation("ProjectMilestone_projectId", fields: [projectId], references: [id])
  assignee User? @relation("ProjectMilestone_assigneeId", fields: [assigneeId], references: [id])
}

model ProjectSupport {
  id String @id @default(cuid())
  projectId String
  userId String
  supportType SupportType
  monthlyAmount Decimal? @db.Decimal(18, 8)
  message String? @db.Text
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation("ProjectSupport_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectSupport_userId", fields: [userId], references: [id])
}

model ProjectApplication {
  id String @id @default(cuid())
  projectId String
  userId String
  roleTitle String
  message String @db.Text
  skills String? @db.Text
  experience String? @db.Text
  availability String?
  status ApplicationStatus @default(PENDING)
  appliedAt DateTime @default(now())
  reviewedAt DateTime?
  reviewMessage String? @db.Text
  project Project @relation("ProjectApplication_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectApplication_userId", fields: [userId], references: [id])
}

model ProjectInvitation {
  id String @id @default(cuid())
  projectId String
  invitedById String
  invitedUserId String?
  invitedEmail String?
  role ProjectRole @default(CONTRIBUTOR)
  message String? @db.Text
  status InvitationStatus @default(PENDING)
  token String? @unique
  expiresAt DateTime?
  createdAt DateTime @default(now())
  respondedAt DateTime?
  project Project @relation("ProjectInvitation_projectId", fields: [projectId], references: [id])
  invitedBy User @relation("ProjectInvitation_invitedById", fields: [invitedById], references: [id])
  invitedUser User? @relation("ProjectInvitation_invitedUserId", fields: [invitedUserId], references: [id])
}

model ProjectMembershipRequest {
  id String @id @default(cuid())
  projectId String
  userId String
  requestedRole ProjectRole @default(CONTRIBUTOR)
  message String @db.Text
  skills String? @db.Text
  motivation String? @db.Text
  portfolio String? @db.Text
  status MembershipRequestStatus @default(PENDING)
  reviewedById String?
  reviewMessage String? @db.Text
  createdAt DateTime @default(now())
  reviewedAt DateTime?
  project Project @relation("ProjectMembershipRequest_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectMembershipRequest_userId", fields: [userId], references: [id])
  reviewedBy User? @relation("ProjectMembershipRequest_reviewedById", fields: [reviewedById], references: [id])
}

model ProjectComment {
  id String @id @default(cuid())
  projectId String
  userId String
  content String @db.Text
  parentId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation("ProjectComment_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectComment_userId", fields: [userId], references: [id])
  parent ProjectComment? @relation("ProjectComment_parentId", fields: [parentId], references: [id])
  parentChildren ProjectComment[] @relation("ProjectComment_parentId")
}

model ProjectUpdate {
  id String @id @default(cuid())
  projectId String
  userId String
  title String
  content String @db.Text
  images String? @db.Text
  createdAt DateTime @default(now())
  project Project @relation("ProjectUpdate_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectUpdate_userId", fields: [userId], references: [id])
}

model Roadmap {
  id String @id @default(cuid())
  projectId String @unique
  title String
  vision String? @db.Text
  status RoadmapStatus @default(DRAFT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  project Project @relation("Roadmap_projectId", fields: [projectId], references: [id])
  assignee User? @relation("Roadmap_assigneeId", fields: [assigneeId], references: [id])
  roadmapPhases RoadmapPhase[] @relation("RoadmapPhase_roadmapId")
}

model RoadmapPhase {
  id String @id @default(cuid())
  roadmapId String
  name String
  description String? @db.Text
  order Int
  startDate DateTime?
  endDate DateTime?
  status PhaseStatus @default(PLANNED)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  roadmap Roadmap @relation("RoadmapPhase_roadmapId", fields: [roadmapId], references: [id])
  assignee User? @relation("RoadmapPhase_assigneeId", fields: [assigneeId], references: [id])
  epics Epic[] @relation("Epic_phaseId")
}

model Epic {
  id String @id @default(cuid())
  phaseId String
  title String
  description String? @db.Text
  status EpicStatus @default(PLANNED)
  priority Priority @default(MEDIUM)
  equityBudget Decimal? @db.Decimal(18, 8)
  progress Int @default(0)
  startDate DateTime?
  targetDate DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  phase RoadmapPhase @relation("Epic_phaseId", fields: [phaseId], references: [id])
  assignee User? @relation("Epic_assigneeId", fields: [assigneeId], references: [id])
  productBacklogItems ProductBacklogItem[] @relation("ProductBacklogItem_epicId")
}

model Sprint {
  id String @id @default(cuid())
  projectId String
  name String
  goal String? @db.Text
  startDate DateTime
  endDate DateTime
  equityBudget Decimal? @db.Decimal(18, 8)
  velocity Int?
  status SprintStatus @default(PLANNED)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  project Project @relation("Sprint_projectId", fields: [projectId], references: [id])
  assignee User? @relation("Sprint_assigneeId", fields: [assigneeId], references: [id])
  sprintItems SprintItem[] @relation("SprintItem_sprintId")
}

model ProductBacklogItem {
  id String @id @default(cuid())
  epicId String
  title String
  description String? @db.Text
  type PBIType @default(FEATURE)
  storyPoints Int?
  status PBIStatus @default(NEW)
  acceptanceCriteria String? @db.Text
  priority Priority @default(MEDIUM)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  epic Epic @relation("ProductBacklogItem_epicId", fields: [epicId], references: [id])
  assignee User? @relation("ProductBacklogItem_assigneeId", fields: [assigneeId], references: [id])
  backlogItems BacklogItem[] @relation("BacklogItem_pbiId")
}

model BacklogItem {
  id String @id @default(cuid())
  pbiId String
  title String
  description String? @db.Text
  type BacklogItemType @default(TASK)
  status BacklogStatus @default(NEW)
  estimate Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assigneeId String?
  projectTasks ProjectTask[] @relation("ProjectTask_backlogItemId")
  pbi ProductBacklogItem @relation("BacklogItem_pbiId", fields: [pbiId], references: [id])
  assignee User? @relation("BacklogItem_assigneeId", fields: [assigneeId], references: [id])
}

model SprintItem {
  id String @id @default(cuid())
  sprintId String
  taskId String
  order Int @default(0)
  addedAt DateTime @default(now())
  sprint Sprint @relation("SprintItem_sprintId", fields: [sprintId], references: [id])
  task ProjectTask @relation("SprintItem_taskId", fields: [taskId], references: [id])
}

model ProjectMember {
  id String @id @default(cuid())
  projectId String
  userId String
  role ProjectRole @default(OBSERVER)
  shareBalance Decimal @default(0) @db.Decimal(18, 8)
  votingPower Decimal @default(0) @db.Decimal(18, 8)
  joinedAt DateTime @default(now())
  invitedById String?
  project Project @relation("ProjectMember_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectMember_userId", fields: [userId], references: [id])
}

model TaskCompensation {
  id String @id @default(cuid())
  taskId String @unique
  compensationModel CompensationModel @default(FIXED_SHARES)
  shareAmount Decimal? @db.Decimal(18, 8)
  hourlyRate Decimal? @db.Decimal(18, 8)
  equityPercent Decimal? @db.Decimal(18, 8)
  stableCoinAmount Decimal? @db.Decimal(18, 8)
  vestingMonths Int?
  createdAt DateTime @default(now())
  task ProjectTask @relation("TaskCompensation_taskId", fields: [taskId], references: [id])
}

model TaskSubmission {
  id String @id @default(cuid())
  taskId String
  submittedById String
  content String @db.Text
  attachments String? @db.Text
  status SubmissionStatus @default(PENDING)
  reviewedById String?
  feedback String? @db.Text
  submittedAt DateTime @default(now())
  reviewedAt DateTime?
  task ProjectTask @relation("TaskSubmission_taskId", fields: [taskId], references: [id])
  submittedBy User @relation("TaskSubmission_submittedById", fields: [submittedById], references: [id])
  reviewedBy User? @relation("TaskSubmission_reviewedById", fields: [reviewedById], references: [id])
}

model Proposal {
  id String @id @default(cuid())
  projectId String
  creatorId String
  type ProposalType
  title String
  description String @db.Text
  options Json
  quorum Int
  threshold Int
  status ProposalStatus @default(DRAFT)
  votingStart DateTime?
  votingEnd DateTime?
  executionDelay Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation("Proposal_projectId", fields: [projectId], references: [id])
  creator User @relation("Proposal_creatorId", fields: [creatorId], references: [id])
  votes Vote[] @relation("Vote_proposalId")
  proposalExecutions ProposalExecution[] @relation("ProposalExecution_proposalId")
}

model Vote {
  id String @id @default(cuid())
  proposalId String
  voterId String
  choice Int
  weight Decimal @db.Decimal(18, 8)
  reason String? @db.Text
  txHash String?
  createdAt DateTime @default(now())
  proposal Proposal @relation("Vote_proposalId", fields: [proposalId], references: [id])
  voter User @relation("Vote_voterId", fields: [voterId], references: [id])
}

model ProposalExecution {
  id String @id @default(cuid())
  proposalId String @unique
  executedAt DateTime @default(now())
  txHash String?
  result Json?
  proposal Proposal @relation("ProposalExecution_proposalId", fields: [proposalId], references: [id])
}

model DelegatedVote {
  id String @id @default(cuid())
  projectId String
  delegatorId String
  delegateeId String
  shareId String
  amount Decimal @db.Decimal(18, 8)
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  expiresAt DateTime?
  revokedAt DateTime?
  project Project @relation("DelegatedVote_projectId", fields: [projectId], references: [id])
  delegator User @relation("DelegatedVote_delegatorId", fields: [delegatorId], references: [id])
  delegatee User @relation("DelegatedVote_delegateeId", fields: [delegateeId], references: [id])
  share ProjectShare @relation("DelegatedVote_shareId", fields: [shareId], references: [id])
}

model Guild {
  id String @id @default(cuid())
  name String
  slug String @unique
  description String @db.Text
  website String?
  email String
  phone String?
  address String? @db.Text
  logo String?
  portfolio String? @db.Text
  specialties String? @db.Text
  isVerified Boolean @default(false)
  rating Decimal? @db.Decimal(18, 8)
  reviewsCount Int @default(0)
  projectsCount Int @default(0)
  membersCount Int @default(0)
  isTrending Boolean @default(false)
  trendingScore Decimal @default(0) @db.Decimal(18, 8)
  trendingRank Int?
  trendingAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ownerId String @unique
  projects Project[] @relation("Project_assignedGuildId")
  owner User @relation("Guild_ownerId", fields: [ownerId], references: [id])
  guildMembers GuildMember[] @relation("GuildMember_guildId")
  guildInvitations GuildInvitation[] @relation("GuildInvitation_guildId")
  guildApplications GuildApplication[] @relation("GuildApplication_guildId")
  projectBids ProjectBid[] @relation("ProjectBid_guildId")
  guildReviews GuildReview[] @relation("GuildReview_guildId")
  guildUpdates GuildUpdate[] @relation("GuildUpdate_guildId")
  taskBids TaskBid[] @relation("TaskBid_guildId")
  posts Post[] @relation("Post_guildId")
  postShares PostShare[] @relation("PostShare_sharedToGuildId")
  events Event[] @relation("Event_guildId")
  guildFollows GuildFollow[] @relation("GuildFollow_guildId")
  opportunities Opportunity[] @relation("Opportunity_guildId")
}

model GuildMember {
  id String @id @default(cuid())
  guildId String
  userId String
  role String
  joinedAt DateTime @default(now())
  guild Guild @relation("GuildMember_guildId", fields: [guildId], references: [id])
  user User @relation("GuildMember_userId", fields: [userId], references: [id])
}

model GuildInvitation {
  id String @id @default(cuid())
  guildId String
  invitedById String
  invitedUserId String?
  invitedEmail String?
  role GuildMemberRole @default(MEMBER)
  message String? @db.Text
  status InvitationStatus @default(PENDING)
  token String? @unique
  expiresAt DateTime?
  createdAt DateTime @default(now())
  respondedAt DateTime?
  guild Guild @relation("GuildInvitation_guildId", fields: [guildId], references: [id])
  invitedBy User @relation("GuildInvitation_invitedById", fields: [invitedById], references: [id])
  invitedUser User? @relation("GuildInvitation_invitedUserId", fields: [invitedUserId], references: [id])
}

model GuildApplication {
  id String @id @default(cuid())
  guildId String
  userId String
  requestedRole GuildMemberRole @default(MEMBER)
  message String @db.Text
  skills String? @db.Text
  experience String? @db.Text
  portfolio String? @db.Text
  availability String?
  status MembershipRequestStatus @default(PENDING)
  reviewedById String?
  reviewMessage String? @db.Text
  appliedAt DateTime @default(now())
  reviewedAt DateTime?
  guild Guild @relation("GuildApplication_guildId", fields: [guildId], references: [id])
  user User @relation("GuildApplication_userId", fields: [userId], references: [id])
  reviewedBy User? @relation("GuildApplication_reviewedById", fields: [reviewedById], references: [id])
}

model ProjectBid {
  id String @id @default(cuid())
  projectId String
  guildId String
  userId String
  proposal String @db.Text
  timeline String? @db.Text
  budget Decimal @db.Decimal(18, 8)
  deliverables String? @db.Text
  status BidStatus @default(SUBMITTED)
  submittedAt DateTime @default(now())
  reviewedAt DateTime?
  project Project @relation("ProjectBid_projectId", fields: [projectId], references: [id])
  guild Guild @relation("ProjectBid_guildId", fields: [guildId], references: [id])
  user User @relation("ProjectBid_userId", fields: [userId], references: [id])
}

model GuildReview {
  id String @id @default(cuid())
  guildId String
  projectId String?
  userId String
  rating Int
  comment String? @db.Text
  createdAt DateTime @default(now())
  guild Guild @relation("GuildReview_guildId", fields: [guildId], references: [id])
  user User @relation("GuildReview_userId", fields: [userId], references: [id])
}

model GuildUpdate {
  id String @id @default(cuid())
  guildId String
  createdById String
  title String
  content String @db.Text
  images String? @db.Text
  createdAt DateTime @default(now())
  guild Guild @relation("GuildUpdate_guildId", fields: [guildId], references: [id])
  createdBy User @relation("GuildUpdate_createdById", fields: [createdById], references: [id])
}

model TaskBid {
  id String @id @default(cuid())
  taskId String
  guildId String
  proposedAmount Decimal? @db.Decimal(18, 8)
  proposal String? @db.Text
  estimatedHours Int?
  status TaskBidStatus @default(SUBMITTED)
  createdAt DateTime @default(now())
  reviewedAt DateTime?
  task ProjectTask @relation("TaskBid_taskId", fields: [taskId], references: [id])
  guild Guild @relation("TaskBid_guildId", fields: [guildId], references: [id])
}

model Shop {
  id String @id @default(cuid())
  name String
  slug String @unique
  description String? @db.Text
  category ShopCategory @default(OTHER)
  address String? @db.Text
  phone String?
  email String?
  website String?
  logo String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ownerId String
  owner User @relation("Shop_ownerId", fields: [ownerId], references: [id])
  products Product[] @relation("Product_shopId")
  invoices Invoice[] @relation("Invoice_shopId")
  sales Sale[] @relation("Sale_shopId")
  inventoryItems InventoryItem[] @relation("InventoryItem_shopId")
  marketingCampaigns MarketingCampaign[] @relation("MarketingCampaign_shopId")
  shopAnalytics ShopAnalytics[] @relation("ShopAnalytics_shopId")
  posts Post[] @relation("Post_shopId")
  opportunities Opportunity[] @relation("Opportunity_shopId")
}

model Product {
  id String @id @default(cuid())
  shopId String
  name String
  description String? @db.Text
  sku String?
  price Decimal @db.Decimal(18, 8)
  cost Decimal? @db.Decimal(18, 8)
  category String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation("Product_shopId", fields: [shopId], references: [id])
  user User @relation("Product_userId", fields: [userId], references: [id])
  saleItems SaleItem[] @relation("SaleItem_productId")
  inventoryItems InventoryItem[] @relation("InventoryItem_productId")
}

model Invoice {
  id String @id @default(cuid())
  shopId String
  buyerId String
  invoiceNumber String @unique
  amount Decimal @db.Decimal(18, 8)
  tax Decimal? @db.Decimal(18, 8)
  discount Decimal? @db.Decimal(18, 8)
  total Decimal @db.Decimal(18, 8)
  status InvoiceStatus @default(DRAFT)
  dueDate DateTime
  paidAt DateTime?
  notes String? @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation("Invoice_shopId", fields: [shopId], references: [id])
  buyer User @relation("Invoice_buyerId", fields: [buyerId], references: [id])
  user User @relation("Invoice_userId", fields: [userId], references: [id])
}

model Sale {
  id String @id @default(cuid())
  shopId String
  buyerId String?
  total Decimal @db.Decimal(18, 8)
  tax Decimal? @db.Decimal(18, 8)
  discount Decimal? @db.Decimal(18, 8)
  paymentMethod PaymentMethod
  notes String? @db.Text
  createdAt DateTime @default(now())
  userId String
  shop Shop @relation("Sale_shopId", fields: [shopId], references: [id])
  buyer User? @relation("Sale_buyerId", fields: [buyerId], references: [id])
  user User @relation("Sale_userId", fields: [userId], references: [id])
  saleItems SaleItem[] @relation("SaleItem_saleId")
}

model SaleItem {
  id String @id @default(cuid())
  saleId String
  productId String
  quantity Int
  price Decimal @db.Decimal(18, 8)
  total Decimal @db.Decimal(18, 8)
  sale Sale @relation("SaleItem_saleId", fields: [saleId], references: [id])
  product Product @relation("SaleItem_productId", fields: [productId], references: [id])
}

model InventoryItem {
  id String @id @default(cuid())
  shopId String
  productId String @unique
  currentStock Int @default(0)
  minStock Int @default(0)
  maxStock Int?
  reorderPoint Int?
  lastRestocked DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation("InventoryItem_shopId", fields: [shopId], references: [id])
  product Product @relation("InventoryItem_productId", fields: [productId], references: [id])
  user User @relation("InventoryItem_userId", fields: [userId], references: [id])
}

model MarketingCampaign {
  id String @id @default(cuid())
  shopId String
  name String
  description String? @db.Text
  platform String
  content String @db.Text
  mediaUrls String? @db.Text
  scheduledAt DateTime?
  status CampaignStatus @default(DRAFT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation("MarketingCampaign_shopId", fields: [shopId], references: [id])
  user User @relation("MarketingCampaign_userId", fields: [userId], references: [id])
}

model ShopAnalytics {
  id String @id @default(cuid())
  shopId String
  date DateTime
  revenue Decimal @db.Decimal(18, 8)
  expenses Decimal @db.Decimal(18, 8)
  profit Decimal @db.Decimal(18, 8)
  salesCount Int
  newCustomers Int
  createdAt DateTime @default(now())
  shop Shop @relation("ShopAnalytics_shopId", fields: [shopId], references: [id])
}

model ProjectShare {
  id String @id @default(cuid())
  projectId String @unique
  assetId String?
  name String
  symbol String
  totalSupply Decimal @db.Decimal(18, 8)
  decimals Int @default(6)
  allocation Json
  vestingConfig Json?
  isDeployed Boolean @default(false)
  deployedAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  delegatedVotes DelegatedVote[] @relation("DelegatedVote_shareId")
  project Project @relation("ProjectShare_projectId", fields: [projectId], references: [id])
  shareHolders ShareHolder[] @relation("ShareHolder_shareId")
  fundraisings Fundraising[] @relation("Fundraising_shareId")
  taskEscrows TaskEscrow[] @relation("TaskEscrow_shareId")
  shareSwaps ShareSwap[] @relation("ShareSwap_fromShareId")
  shareSwaps1 ShareSwap[] @relation("ShareSwap_toShareId")
  liquidityPools LiquidityPool[] @relation("LiquidityPool_share1Id")
  liquidityPools1 LiquidityPool[] @relation("LiquidityPool_share2Id")
}

model ProjectEquity {
  id String @id @default(cuid())
  projectId String
  userId String
  sharePercent Decimal @db.Decimal(18, 8)
  investmentAmount Decimal @db.Decimal(18, 8)
  grantedAt DateTime @default(now())
  project Project @relation("ProjectEquity_projectId", fields: [projectId], references: [id])
  user User @relation("ProjectEquity_userId", fields: [userId], references: [id])
}

model Treasury {
  id String @id @default(cuid())
  projectId String @unique
  balance Decimal @default(0) @db.Decimal(18, 8)
  shareAssetId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation("Treasury_projectId", fields: [projectId], references: [id])
  treasuryTransactions TreasuryTransaction[] @relation("TreasuryTransaction_treasuryId")
}

model TreasuryTransaction {
  id String @id @default(cuid())
  treasuryId String
  type TransactionType
  amount Decimal @db.Decimal(18, 8)
  description String?
  txHash String?
  proposalId String?
  createdAt DateTime @default(now())
  treasury Treasury @relation("TreasuryTransaction_treasuryId", fields: [treasuryId], references: [id])
}

model ShareHolder {
  id String @id @default(cuid())
  shareId String
  userId String
  balance Decimal @db.Decimal(18, 8)
  stakedAmount Decimal @default(0) @db.Decimal(18, 8)
  lockedAmount Decimal @default(0) @db.Decimal(18, 8)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  share ProjectShare @relation("ShareHolder_shareId", fields: [shareId], references: [id])
  user User @relation("ShareHolder_userId", fields: [userId], references: [id])
  shareVestings ShareVesting[] @relation("ShareVesting_holderId")
}

model ShareVesting {
  id String @id @default(cuid())
  holderId String
  totalAmount Decimal @db.Decimal(18, 8)
  releasedAmount Decimal @default(0) @db.Decimal(18, 8)
  startDate DateTime
  cliffEnd DateTime
  vestingEnd DateTime
  releaseFrequency VestingFrequency @default(MONTHLY)
  createdAt DateTime @default(now())
  holder ShareHolder @relation("ShareVesting_holderId", fields: [holderId], references: [id])
}

model Fundraising {
  id String @id @default(cuid())
  shareId String @unique
  fundingGoal Decimal @db.Decimal(18, 8)
  currentFunding Decimal @default(0) @db.Decimal(18, 8)
  minContribution Decimal? @db.Decimal(18, 8)
  maxContribution Decimal? @db.Decimal(18, 8)
  sharePrice Decimal @db.Decimal(18, 8)
  startDate DateTime
  endDate DateTime
  status FundraisingStatus @default(PENDING)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  share ProjectShare @relation("Fundraising_shareId", fields: [shareId], references: [id])
  fundraisingContributions FundraisingContribution[] @relation("FundraisingContribution_fundraisingId")
}

model FundraisingContribution {
  id String @id @default(cuid())
  fundraisingId String
  userId String
  amount Decimal @db.Decimal(18, 8)
  shareAmount Decimal @db.Decimal(18, 8)
  paymentAsset String
  txHash String?
  status ContributionStatus @default(PENDING)
  createdAt DateTime @default(now())
  fundraising Fundraising @relation("FundraisingContribution_fundraisingId", fields: [fundraisingId], references: [id])
  user User @relation("FundraisingContribution_userId", fields: [userId], references: [id])
}

model Wallet {
  id String @id @default(cuid())
  userId String
  address String @unique
  provider WalletProvider
  label String?
  isVerified Boolean @default(false)
  isPrimary Boolean @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user User @relation("Wallet_userId", fields: [userId], references: [id])
}

model TaskEscrow {
  id String @id @default(cuid())
  taskId String @unique
  funderId String
  shareId String
  amount Decimal @db.Decimal(18, 8)
  status EscrowStatus @default(FUNDED)
  txHashFund String?
  txHashRelease String?
  txHashRefund String?
  createdAt DateTime @default(now())
  fundedAt DateTime?
  releasedAt DateTime?
  refundedAt DateTime?
  task ProjectTask @relation("TaskEscrow_taskId", fields: [taskId], references: [id])
  funder User @relation("TaskEscrow_funderId", fields: [funderId], references: [id])
  share ProjectShare @relation("TaskEscrow_shareId", fields: [shareId], references: [id])
}

model ShareSwap {
  id String @id @default(cuid())
  userId String
  fromShareId String
  toShareId String
  fromAmount Decimal @db.Decimal(18, 8)
  toAmount Decimal @db.Decimal(18, 8)
  exchangeRate Decimal @db.Decimal(18, 8)
  fee Decimal @default(0) @db.Decimal(18, 8)
  txHash String?
  status SwapStatus @default(PENDING)
  createdAt DateTime @default(now())
  completedAt DateTime?
  user User @relation("ShareSwap_userId", fields: [userId], references: [id])
  fromShare ProjectShare @relation("ShareSwap_fromShareId", fields: [fromShareId], references: [id])
  toShare ProjectShare @relation("ShareSwap_toShareId", fields: [toShareId], references: [id])
}

model LiquidityPool {
  id String @id @default(cuid())
  share1Id String
  share2Id String
  reserve1 Decimal @default(0) @db.Decimal(18, 8)
  reserve2 Decimal @default(0) @db.Decimal(18, 8)
  totalShares Decimal @default(0) @db.Decimal(18, 8)
  feePercent Decimal @default(0.003) @db.Decimal(18, 8)
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  share1 ProjectShare @relation("LiquidityPool_share1Id", fields: [share1Id], references: [id])
  share2 ProjectShare @relation("LiquidityPool_share2Id", fields: [share2Id], references: [id])
  liquidityProviders LiquidityProvider[] @relation("LiquidityProvider_poolId")
}

model LiquidityProvider {
  id String @id @default(cuid())
  poolId String
  userId String
  shares Decimal @db.Decimal(18, 8)
  share1In Decimal @default(0) @db.Decimal(18, 8)
  share2In Decimal @default(0) @db.Decimal(18, 8)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  pool LiquidityPool @relation("LiquidityProvider_poolId", fields: [poolId], references: [id])
  user User @relation("LiquidityProvider_userId", fields: [userId], references: [id])
}

model Post {
  id String @id @default(cuid())
  authorId String
  projectId String?
  guildId String?
  shopId String?
  type PostType @default(POST)
  visibility PostVisibility @default(PUBLIC)
  title String?
  content String @db.Text
  metadata Json?
  likesCount Int @default(0)
  commentsCount Int @default(0)
  sharesCount Int @default(0)
  viewsCount Int @default(0)
  isPinned Boolean @default(false)
  isTrending Boolean @default(false)
  trendingScore Decimal @default(0) @db.Decimal(18, 8)
  trendingRank Int?
  trendingAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  author User @relation("Post_authorId", fields: [authorId], references: [id])
  project Project? @relation("Post_projectId", fields: [projectId], references: [id])
  guild Guild? @relation("Post_guildId", fields: [guildId], references: [id])
  shop Shop? @relation("Post_shopId", fields: [shopId], references: [id])
  postMedias PostMedia[] @relation("PostMedia_postId")
  postLikes PostLike[] @relation("PostLike_postId")
  postComments PostComment[] @relation("PostComment_postId")
  postBookmarks PostBookmark[] @relation("PostBookmark_postId")
  postShares PostShare[] @relation("PostShare_postId")
}

model PostMedia {
  id String @id @default(cuid())
  postId String
  type MimeType
  url String
  thumbnailUrl String?
  altText String?
  order Int @default(0)
  createdAt DateTime @default(now())
  post Post @relation("PostMedia_postId", fields: [postId], references: [id])
}

model PostLike {
  id String @id @default(cuid())
  postId String
  userId String
  createdAt DateTime @default(now())
  post Post @relation("PostLike_postId", fields: [postId], references: [id])
  user User @relation("PostLike_userId", fields: [userId], references: [id])
}

model PostComment {
  id String @id @default(cuid())
  postId String
  authorId String
  parentId String?
  content String @db.Text
  likesCount Int @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  post Post @relation("PostComment_postId", fields: [postId], references: [id])
  author User @relation("PostComment_authorId", fields: [authorId], references: [id])
  parent PostComment? @relation("PostComment_parentId", fields: [parentId], references: [id])
  parentChildren PostComment[] @relation("PostComment_parentId")
}

model PostBookmark {
  id String @id @default(cuid())
  postId String
  userId String
  createdAt DateTime @default(now())
  post Post @relation("PostBookmark_postId", fields: [postId], references: [id])
  user User @relation("PostBookmark_userId", fields: [userId], references: [id])
}

model PostShare {
  id String @id @default(cuid())
  postId String
  userId String
  sharedToProjectId String?
  sharedToGuildId String?
  comment String? @db.Text
  createdAt DateTime @default(now())
  post Post @relation("PostShare_postId", fields: [postId], references: [id])
  user User @relation("PostShare_userId", fields: [userId], references: [id])
  sharedToProject Project? @relation("PostShare_sharedToProjectId", fields: [sharedToProjectId], references: [id])
  sharedToGuild Guild? @relation("PostShare_sharedToGuildId", fields: [sharedToGuildId], references: [id])
}

model Conversation {
  id String @id @default(cuid())
  type ConversationType
  name String?
  avatarUrl String?
  createdById String?
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
  lastMessageAt DateTime?
  createdBy User? @relation("Conversation_createdById", fields: [createdById], references: [id], onDelete: SetNull)
  conversationMembers ConversationMember[] @relation("ConversationMember_conversationId")
  chatMessages ChatMessage[] @relation("ChatMessage_conversationId")

  @@index([createdById])
}

model ConversationMember {
  id String @id @default(cuid())
  conversationId String
  userId String
  role ConversationRole @default(MEMBER)
  lastReadAt DateTime?
  joinedAt DateTime @default(now())
  lastActiveAt DateTime?
  conversation Conversation @relation("ConversationMember_conversationId", fields: [conversationId], references: [id], onDelete: Cascade)
  user User @relation("ConversationMember_userId", fields: [userId], references: [id], onDelete: Cascade)

  @@unique([conversationId, userId])
}

model ChatMessage {
  id String @id @default(cuid())
  userToId String
  userFromId String
  message String?
  status MessageStatus @default(NOTSEEN)
  ChatReaction Json?
  chatAttachmentId String?
  sentAt DateTime @default(now())
  seenAt DateTime?
  conversationId String?
  replyToId String?
  deliveredAt DateTime?
  isDeleted Boolean @default(false)
  editedAt DateTime?
  userTo User @relation("ChatMessage_userToId", fields: [userToId], references: [id])
  userFrom User @relation("ChatMessage_userFromId", fields: [userFromId], references: [id])
  chatAttachment Attachment? @relation("ChatMessage_chatAttachmentId", fields: [chatAttachmentId], references: [id])
  conversation Conversation? @relation("ChatMessage_conversationId", fields: [conversationId], references: [id], onDelete: Cascade)
  replyTo ChatMessage? @relation("ChatMessage_replyToId", fields: [replyToId], references: [id], onDelete: Restrict)
  replyToChildren ChatMessage[] @relation("ChatMessage_replyToId")

  @@index([conversationId, sentAt])
}

model Attachment {
  id String @id @default(cuid())
  uploadedById String
  bucketPath String?
  type MimeType
  createdAt DateTime @default(now())
  lastUsedAt DateTime?
  chatMessages ChatMessage[] @relation("ChatMessage_chatAttachmentId")
  uploadedBy User @relation("Attachment_uploadedById", fields: [uploadedById], references: [id])
}

model Event {
  id String @id @default(cuid())
  title String
  slug String @unique
  description String? @db.Text
  type EventType @default(MEETING)
  visibility EventVisibility @default(PUBLIC)
  status EventStatus @default(SCHEDULED)
  location String?
  locationUrl String?
  isOnline Boolean @default(false)
  meetingUrl String?
  timezone String @default("UTC")
  startDate DateTime
  endDate DateTime
  maxAttendees Int?
  attendeesCount Int @default(0)
  coverImage String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  organizerId String
  projectId String?
  guildId String?
  organizer User @relation("Event_organizerId", fields: [organizerId], references: [id])
  project Project? @relation("Event_projectId", fields: [projectId], references: [id])
  guild Guild? @relation("Event_guildId", fields: [guildId], references: [id])
  eventAttendees EventAttendee[] @relation("EventAttendee_eventId")
  eventCoHosts EventCoHost[] @relation("EventCoHost_eventId")
  eventReminders EventReminder[] @relation("EventReminder_eventId")
}

model EventAttendee {
  id String @id @default(cuid())
  eventId String
  userId String
  status AttendeeStatus @default(GOING)
  rsvpAt DateTime @default(now())
  attendedAt DateTime?
  notes String? @db.Text
  event Event @relation("EventAttendee_eventId", fields: [eventId], references: [id])
  user User @relation("EventAttendee_userId", fields: [userId], references: [id])
}

model EventCoHost {
  id String @id @default(cuid())
  eventId String
  userId String
  addedAt DateTime @default(now())
  event Event @relation("EventCoHost_eventId", fields: [eventId], references: [id])
  user User @relation("EventCoHost_userId", fields: [userId], references: [id])
}

model EventReminder {
  id String @id @default(cuid())
  eventId String
  userId String
  remindAt DateTime
  isSent Boolean @default(false)
  sentAt DateTime?
  createdAt DateTime @default(now())
  event Event @relation("EventReminder_eventId", fields: [eventId], references: [id])
  user User @relation("EventReminder_userId", fields: [userId], references: [id])
}

model UserFollow {
  id String @id @default(cuid())
  followerId String
  followingId String
  createdAt DateTime @default(now())
  follower User @relation("UserFollow_followerId", fields: [followerId], references: [id])
  following User @relation("UserFollow_followingId", fields: [followingId], references: [id])
}

model ProjectFollow {
  id String @id @default(cuid())
  userId String
  projectId String
  notifyUpdates Boolean @default(true)
  notifyMilestones Boolean @default(true)
  notifyProposals Boolean @default(false)
  createdAt DateTime @default(now())
  user User @relation("ProjectFollow_userId", fields: [userId], references: [id])
  project Project @relation("ProjectFollow_projectId", fields: [projectId], references: [id])
}

model GuildFollow {
  id String @id @default(cuid())
  userId String
  guildId String
  notifyUpdates Boolean @default(true)
  notifyEvents Boolean @default(true)
  notifyProjects Boolean @default(false)
  createdAt DateTime @default(now())
  user User @relation("GuildFollow_userId", fields: [userId], references: [id])
  guild Guild @relation("GuildFollow_guildId", fields: [guildId], references: [id])
}

model Opportunity {
  id String @id @default(cuid())
  title String
  slug String @unique
  description String @db.Text
  type OpportunityType
  status OpportunityStatus @default(DRAFT)
  experienceLevel ExperienceLevel @default(MID)
  requirements String? @db.Text
  skills String? @db.Text
  benefits String? @db.Text
  location String?
  isRemote Boolean @default(true)
  compensation Decimal? @db.Decimal(18, 8)
  compensationDetails String?
  deadline DateTime?
  maxApplications Int?
  applicationsCount Int @default(0)
  coverImage String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  closedAt DateTime?
  posterId String
  guildId String?
  projectId String?
  shopId String?
  taskId String?
  poster User @relation("Opportunity_posterId", fields: [posterId], references: [id])
  guild Guild? @relation("Opportunity_guildId", fields: [guildId], references: [id])
  project Project? @relation("Opportunity_projectId", fields: [projectId], references: [id])
  shop Shop? @relation("Opportunity_shopId", fields: [shopId], references: [id])
  task ProjectTask? @relation("Opportunity_taskId", fields: [taskId], references: [id])
  opportunityApplications OpportunityApplication[] @relation("OpportunityApplication_opportunityId")
  opportunityUpdates OpportunityUpdate[] @relation("OpportunityUpdate_opportunityId")
  opportunityComments OpportunityComment[] @relation("OpportunityComment_opportunityId")
}

model OpportunityApplication {
  id String @id @default(cuid())
  opportunityId String
  applicantId String
  coverLetter String @db.Text
  portfolio String? @db.Text
  additionalInfo String? @db.Text
  status ApplicationStatus @default(PENDING)
  reviewNotes String? @db.Text
  appliedAt DateTime @default(now())
  reviewedAt DateTime?
  opportunity Opportunity @relation("OpportunityApplication_opportunityId", fields: [opportunityId], references: [id])
  applicant User @relation("OpportunityApplication_applicantId", fields: [applicantId], references: [id])

  @@unique([opportunityId, applicantId])
}

model OpportunityUpdate {
  id String @id @default(cuid())
  opportunityId String
  userId String
  title String
  content String @db.Text
  images String? @db.Text
  createdAt DateTime @default(now())
  opportunity Opportunity @relation("OpportunityUpdate_opportunityId", fields: [opportunityId], references: [id])
  user User @relation("OpportunityUpdate_userId", fields: [userId], references: [id])
}

model OpportunityComment {
  id String @id @default(cuid())
  opportunityId String
  userId String
  content String @db.Text
  parentId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  opportunity Opportunity @relation("OpportunityComment_opportunityId", fields: [opportunityId], references: [id])
  user User @relation("OpportunityComment_userId", fields: [userId], references: [id])
  parent OpportunityComment? @relation("OpportunityComment_parentId", fields: [parentId], references: [id])
  parentChildren OpportunityComment[] @relation("OpportunityComment_parentId")
}

