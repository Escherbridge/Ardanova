// Auto-generated from database-archietecture.dbml
// DO NOT EDIT DIRECTLY - modify the DBML file and run: npm run generate:prisma

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

generator zod {
  provider         = "zod-prisma-types"
  output           = "../src/lib/zod"
  useMultipleFiles = true
  createInputTypes = false
}

datasource db {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

enum UserRole {
  INDIVIDUAL
  GUILD
  ADMIN
}

enum UserType {
  INNOVATOR
  SUPPORTER
  VOLUNTEER
  FREELANCER
  SME_OWNER
  GUILD_MEMBER
}

enum UserTier {
  BRONZE
  SILVER
  GOLD
  PLATINUM
  DIAMOND
}

enum VerificationLevel {
  ANONYMOUS
  VERIFIED
  PRO
  EXPERT
}

enum NotificationType {
  TASK_ASSIGNED
  TASK_COMPLETED
  TASK_REVIEWED
  TASK_REVISION_REQUESTED
  PROPOSAL_CREATED
  PROPOSAL_VOTED
  PROPOSAL_PASSED
  PROPOSAL_REJECTED
  PROPOSAL_EXECUTED
  ACHIEVEMENT_EARNED
  LEVEL_UP
  STREAK_MILESTONE
  TOKEN_RECEIVED
  TOKEN_VESTED
  ESCROW_FUNDED
  ESCROW_RELEASED
  PROJECT_INVITATION
  PROJECT_UPDATE
  PROJECT_FUNDED
  COMMENT_REPLY
  MENTION
  FOLLOWER_NEW
  SYSTEM_ANNOUNCEMENT
  SECURITY_ALERT
}

enum ActivityType {
  CREATED
  UPDATED
  DELETED
  COMPLETED
  JOINED
  LEFT
  COMMENTED
  VOTED
  SUBMITTED
  REVIEWED
  FUNDED
  TRANSFERRED
  SWAPPED
}

enum XPEventType {
  TASK_COMPLETED
  PROPOSAL_CREATED
  PROPOSAL_PASSED
  VOTE_CAST
  PROJECT_FUNDED
  MEMBER_REFERRED
  ACHIEVEMENT_EARNED
  STREAK_MAINTAINED
  LEVEL_UP
  REVIEW_GIVEN
  CONTRIBUTION_MADE
}

enum AchievementCategory {
  CONTRIBUTOR
  COLLABORATOR
  INVESTOR
  GOVERNANCE
  COMMUNITY
  STREAK
  MILESTONE
  GAMING
}

enum AchievementRarity {
  COMMON
  UNCOMMON
  RARE
  EPIC
  LEGENDARY
}

enum LeaderboardPeriod {
  DAILY
  WEEKLY
  MONTHLY
  QUARTERLY
  ALL_TIME
}

enum LeaderboardCategory {
  XP
  TASKS_COMPLETED
  TOKENS_EARNED
  PROPOSALS_CREATED
  VOTES_CAST
  PROJECTS_FUNDED
}

enum StreakType {
  DAILY_LOGIN
  DAILY_CONTRIBUTION
  WEEKLY_TASK
}

enum ReferralStatus {
  PENDING
  COMPLETED
  EXPIRED
  CANCELLED
}

enum ProjectStatus {
  DRAFT
  PUBLISHED
  SEEKING_SUPPORT
  FUNDED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum ProjectCategory {
  TECHNOLOGY
  HEALTHCARE
  EDUCATION
  ENVIRONMENT
  SOCIAL_IMPACT
  BUSINESS
  ARTS_CULTURE
  AGRICULTURE
  FINANCE
  OTHER
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  REVIEW
  COMPLETED
  BLOCKED
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum SupportType {
  VOTE
  SUBSCRIPTION
  VOLUNTEER
  RESOURCE
}

enum ApplicationStatus {
  PENDING
  ACCEPTED
  REJECTED
  WITHDRAWN
}

enum RoadmapStatus {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED
}

enum PhaseStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum EpicStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum SprintStatus {
  PLANNED
  ACTIVE
  COMPLETED
  CANCELLED
}

enum PBIType {
  FEATURE
  ENHANCEMENT
  BUG
  TECHNICAL_DEBT
  SPIKE
}

enum PBIStatus {
  NEW
  READY
  IN_PROGRESS
  DONE
  CANCELLED
}

enum BacklogItemType {
  TASK
  BUG
  IMPROVEMENT
  RESEARCH
}

enum BacklogStatus {
  NEW
  READY
  IN_PROGRESS
  DONE
  BLOCKED
}

enum Priority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ProjectRole {
  FOUNDER
  LEADER
  CORE_CONTRIBUTOR
  CONTRIBUTOR
  OBSERVER
}

enum CompensationModel {
  FIXED_TOKEN
  HOURLY_TOKEN
  EQUITY_PERCENT
  HYBRID
  BOUNTY
  MILESTONE
}

enum SubmissionStatus {
  PENDING
  APPROVED
  REJECTED
  REVISION_REQUESTED
}

enum ProposalType {
  TREASURY
  GOVERNANCE
  STRATEGIC
  OPERATIONAL
  EMERGENCY
  CONSTITUTIONAL
  TOKEN
}

enum ProposalStatus {
  DRAFT
  ACTIVE
  PASSED
  REJECTED
  EXECUTED
  CANCELLED
  EXPIRED
}

enum BidStatus {
  SUBMITTED
  UNDER_REVIEW
  ACCEPTED
  REJECTED
  WITHDRAWN
}

enum InvoiceStatus {
  DRAFT
  SENT
  PAID
  OVERDUE
  CANCELLED
}

enum PaymentMethod {
  BANK_TRANSFER
  USSD
  CARD
  WALLET
}

enum CampaignStatus {
  DRAFT
  SCHEDULED
  ACTIVE
  COMPLETED
  CANCELLED
}

enum EscrowStatus {
  NONE
  FUNDED
  RELEASED
  DISPUTED
  REFUNDED
}

enum TransactionType {
  DEPOSIT
  WITHDRAWAL
  TASK_PAYMENT
  PROPOSAL_EXECUTION
  DIVIDEND
  FEE
}

enum VestingFrequency {
  DAILY
  WEEKLY
  MONTHLY
  QUARTERLY
}

enum ICOStatus {
  PENDING
  ACTIVE
  SUCCESSFUL
  FAILED
  CANCELLED
  REFUNDING
}

enum ContributionStatus {
  PENDING
  CONFIRMED
  REFUNDED
  FAILED
}

enum WalletProvider {
  PERA
  DEFLY
  ALGOSIGNER
  WALLETCONNECT
  OTHER
}

enum SwapStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
}

enum MessageStatus {
  SENT
  DELIVERED
  READ
  FAILED
  NOTSEEN
}

enum MimeType {
  IMAGE
  VIDEO
  AUDIO
  DOCUMENT
  ARCHIVE
  OTHER
}

model Account {
  id String @id @default(cuid())
  userId String
  type String
  provider String
  providerAccountId String
  refresh_token String? @db.Text
  access_token String? @db.Text
  expires_at Int?
  token_type String?
  scope String?
  id_token String? @db.Text
  session_state String?
  user User @relation(fields: [userId], references: [id])
}

model Session {
  id String @id @default(cuid())
  sessionToken String @unique
  userId String
  expires DateTime
  user User @relation(fields: [userId], references: [id])
}

model User {
  id String @id @default(cuid())
  email String @unique
  emailVerified DateTime?
  name String?
  image String?
  bio String? @db.Text
  location String?
  phone String?
  website String?
  linkedIn String?
  twitter String?
  role UserRole @default("INDIVIDUAL")
  userType UserType @default("INNOVATOR")
  isVerified Boolean @default(false)
  totalXP Int @default(0)
  level Int @default(1)
  tier UserTier @default("BRONZE")
  trustScore Decimal @default(0)
  verificationLevel VerificationLevel @default("ANONYMOUS")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  accounts Account[]
  sessions Session[]
  userSkills UserSkill[]
  userExperiences UserExperience[]
  verificationTokens VerificationToken[]
  notifications Notification[]
  activities Activity[]
  xPEvents XPEvent[]
  userAchievements UserAchievement[]
  leaderboardEntries LeaderboardEntry[]
  userStreaks UserStreak[]
  referrals Referral[]
  referrals1 Referral[]
  projects Project[]
  projectTasks ProjectTask[]
  projectSupports ProjectSupport[]
  projectApplications ProjectApplication[]
  projectComments ProjectComment[]
  projectUpdates ProjectUpdate[]
  projectMembers ProjectMember[]
  taskSubmissions TaskSubmission[]
  taskSubmissions1 TaskSubmission[]
  proposals Proposal[]
  votes Vote[]
  delegatedVotes DelegatedVote[]
  delegatedVotes1 DelegatedVote[]
  guilds Guild[]
  guildMembers GuildMember[]
  projectBids ProjectBid[]
  guildReviews GuildReview[]
  shops Shop[]
  products Product[]
  invoices Invoice[]
  invoices1 Invoice[]
  sales Sale[]
  sales1 Sale[]
  inventoryItems InventoryItem[]
  marketingCampaigns MarketingCampaign[]
  projectEquities ProjectEquity[]
  tokenHolders TokenHolder[]
  iCOContributions ICOContribution[]
  wallets Wallet[]
  taskEscrows TaskEscrow[]
  tokenSwaps TokenSwap[]
  liquidityProviders LiquidityProvider[]
  chatMessages ChatMessage[]
  chatMessages1 ChatMessage[]
  attachments Attachment[]
}

model UserSkill {
  id String @id @default(cuid())
  userId String
  skill String
  level Int @default(1)
  user User @relation(fields: [userId], references: [id])
}

model UserExperience {
  id String @id @default(cuid())
  userId String
  title String
  company String
  description String? @db.Text
  startDate DateTime
  endDate DateTime?
  isCurrent Boolean @default(false)
  user User @relation(fields: [userId], references: [id])
}

model VerificationToken {
  identifier String
  token String @unique
  expires DateTime
  user User @relation(fields: [identifier], references: [id])
}

model Notification {
  id String @id @default(cuid())
  userId String
  type NotificationType
  title String
  message String @db.Text
  data Json?
  isRead Boolean @default(false)
  readAt DateTime?
  actionUrl String?
  createdAt DateTime @default(now())
  user User @relation(fields: [userId], references: [id])
}

model Activity {
  id String @id @default(cuid())
  userId String
  projectId String?
  type ActivityType
  entityType String
  entityId String
  action String
  metadata Json?
  createdAt DateTime @default(now())
  user User @relation(fields: [userId], references: [id])
  project Project @relation(fields: [projectId], references: [id])
}

model XPEvent {
  id String @id @default(cuid())
  userId String
  eventType XPEventType
  amount Int
  source String
  sourceId String?
  metadata Json?
  createdAt DateTime @default(now())
  user User @relation(fields: [userId], references: [id])
}

model Achievement {
  id String @id @default(cuid())
  name String @unique
  description String
  category AchievementCategory
  criteria Json
  xpReward Int
  tokenReward Decimal?
  rarity AchievementRarity @default("COMMON")
  icon String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  userAchievements UserAchievement[]
}

model UserAchievement {
  id String @id @default(cuid())
  userId String
  achievementId String
  progress Int @default(0)
  earnedAt DateTime?
  user User @relation(fields: [userId], references: [id])
  achievement Achievement @relation(fields: [achievementId], references: [id])
}

model Leaderboard {
  id String @id @default(cuid())
  period LeaderboardPeriod
  category LeaderboardCategory
  startDate DateTime
  endDate DateTime
  createdAt DateTime @default(now())
  leaderboardEntries LeaderboardEntry[]
}

model LeaderboardEntry {
  id String @id @default(cuid())
  leaderboardId String
  userId String
  rank Int
  score Int
  metadata Json?
  leaderboard Leaderboard @relation(fields: [leaderboardId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model UserStreak {
  id String @id @default(cuid())
  userId String @unique
  currentStreak Int @default(0)
  longestStreak Int @default(0)
  lastActivityDate DateTime?
  streakType StreakType @default("DAILY_LOGIN")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user User @relation(fields: [userId], references: [id])
}

model Referral {
  id String @id @default(cuid())
  referrerId String
  referredId String @unique
  referralCode String?
  status ReferralStatus @default("PENDING")
  rewardClaimed Boolean @default(false)
  xpRewarded Int?
  tokenRewarded Decimal?
  createdAt DateTime @default(now())
  completedAt DateTime?
  referrer User @relation(fields: [referrerId], references: [id])
  referred User @relation(fields: [referredId], references: [id])
}

model Project {
  id String @id @default(cuid())
  title String
  slug String @unique
  description String @db.Text
  problemStatement String @db.Text
  solution String @db.Text
  category ProjectCategory
  status ProjectStatus @default("DRAFT")
  fundingGoal Decimal?
  currentFunding Decimal @default(0)
  supportersCount Int @default(0)
  votesCount Int @default(0)
  viewsCount Int @default(0)
  featured Boolean @default(false)
  tags String? @db.Text
  images String? @db.Text
  videos String? @db.Text
  documents String? @db.Text
  targetAudience String? @db.Text
  expectedImpact String? @db.Text
  timeline String? @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  publishedAt DateTime?
  fundedAt DateTime?
  completedAt DateTime?
  createdById String
  assignedGuildId String?
  activities Activity[]
  createdBy User @relation(fields: [createdById], references: [id])
  assignedGuild Guild @relation(fields: [assignedGuildId], references: [id])
  projectTasks ProjectTask[]
  projectResources ProjectResource[]
  projectMilestones ProjectMilestone[]
  projectSupports ProjectSupport[]
  projectApplications ProjectApplication[]
  projectComments ProjectComment[]
  projectUpdates ProjectUpdate[]
  roadmaps Roadmap[]
  sprints Sprint[]
  projectMembers ProjectMember[]
  proposals Proposal[]
  delegatedVotes DelegatedVote[]
  projectBids ProjectBid[]
  projectTokens ProjectToken[]
  projectEquities ProjectEquity[]
  treasuries Treasury[]
}

model ProjectTask {
  id String @id @default(cuid())
  projectId String
  backlogItemId String?
  title String
  description String? @db.Text
  status TaskStatus @default("TODO")
  priority TaskPriority @default("MEDIUM")
  estimatedHours Int?
  actualHours Int?
  tokenReward Decimal?
  escrowStatus EscrowStatus @default("NONE")
  dueDate DateTime?
  completedAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  assignedToId String?
  project Project @relation(fields: [projectId], references: [id])
  assignedTo User @relation(fields: [assignedToId], references: [id])
  backlogItem BacklogItem @relation(fields: [backlogItemId], references: [id])
  projectTaskDependencies ProjectTaskDependency[]
  projectTaskDependencies1 ProjectTaskDependency[]
  sprintItems SprintItem[]
  taskCompensations TaskCompensation[]
  taskSubmissions TaskSubmission[]
  taskEscrows TaskEscrow[]
}

model ProjectTaskDependency {
  id String @id @default(cuid())
  taskId String
  dependsOnId String
  task ProjectTask @relation(fields: [taskId], references: [id])
  dependsOn ProjectTask @relation(fields: [dependsOnId], references: [id])
}

model ProjectResource {
  id String @id @default(cuid())
  projectId String
  name String
  description String? @db.Text
  quantity Int @default(1)
  estimatedCost Decimal?
  isRequired Boolean @default(true)
  isObtained Boolean @default(false)
  createdAt DateTime @default(now())
  project Project @relation(fields: [projectId], references: [id])
}

model ProjectMilestone {
  id String @id @default(cuid())
  projectId String
  title String
  description String? @db.Text
  targetDate DateTime
  completedAt DateTime?
  isCompleted Boolean @default(false)
  createdAt DateTime @default(now())
  project Project @relation(fields: [projectId], references: [id])
}

model ProjectSupport {
  id String @id @default(cuid())
  projectId String
  userId String
  supportType SupportType
  monthlyAmount Decimal?
  message String? @db.Text
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model ProjectApplication {
  id String @id @default(cuid())
  projectId String
  userId String
  roleTitle String
  message String @db.Text
  skills String? @db.Text
  experience String? @db.Text
  availability String?
  status ApplicationStatus @default("PENDING")
  appliedAt DateTime @default(now())
  reviewedAt DateTime?
  reviewMessage String? @db.Text
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model ProjectComment {
  id String @id @default(cuid())
  projectId String
  userId String
  content String @db.Text
  parentId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
  parent ProjectComment @relation(fields: [parentId], references: [id])
}

model ProjectUpdate {
  id String @id @default(cuid())
  projectId String
  userId String
  title String
  content String @db.Text
  images String? @db.Text
  createdAt DateTime @default(now())
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model Roadmap {
  id String @id @default(cuid())
  projectId String @unique
  title String
  vision String? @db.Text
  status RoadmapStatus @default("DRAFT")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  roadmapPhases RoadmapPhase[]
}

model RoadmapPhase {
  id String @id @default(cuid())
  roadmapId String
  name String
  description String? @db.Text
  order Int
  startDate DateTime?
  endDate DateTime?
  status PhaseStatus @default("PLANNED")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  roadmap Roadmap @relation(fields: [roadmapId], references: [id])
  epics Epic[]
}

model Epic {
  id String @id @default(cuid())
  phaseId String
  title String
  description String? @db.Text
  status EpicStatus @default("PLANNED")
  priority Priority @default("MEDIUM")
  tokenBudget Decimal?
  progress Int @default(0)
  startDate DateTime?
  targetDate DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  phase RoadmapPhase @relation(fields: [phaseId], references: [id])
  productBacklogItems ProductBacklogItem[]
}

model Sprint {
  id String @id @default(cuid())
  projectId String
  name String
  goal String? @db.Text
  startDate DateTime
  endDate DateTime
  tokenBudget Decimal?
  velocity Int?
  status SprintStatus @default("PLANNED")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  sprintItems SprintItem[]
}

model ProductBacklogItem {
  id String @id @default(cuid())
  epicId String
  title String
  description String? @db.Text
  type PBIType @default("FEATURE")
  storyPoints Int?
  status PBIStatus @default("NEW")
  acceptanceCriteria String? @db.Text
  priority Priority @default("MEDIUM")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  epic Epic @relation(fields: [epicId], references: [id])
  backlogItems BacklogItem[]
}

model BacklogItem {
  id String @id @default(cuid())
  pbiId String
  title String
  description String? @db.Text
  type BacklogItemType @default("TASK")
  status BacklogStatus @default("NEW")
  estimate Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  projectTasks ProjectTask[]
  pbi ProductBacklogItem @relation(fields: [pbiId], references: [id])
}

model SprintItem {
  id String @id @default(cuid())
  sprintId String
  taskId String
  order Int @default(0)
  addedAt DateTime @default(now())
  sprint Sprint @relation(fields: [sprintId], references: [id])
  task ProjectTask @relation(fields: [taskId], references: [id])
}

model ProjectMember {
  id String @id @default(cuid())
  projectId String
  userId String
  role ProjectRole @default("OBSERVER")
  tokenBalance Decimal @default(0)
  votingPower Decimal @default(0)
  joinedAt DateTime @default(now())
  invitedById String?
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model TaskCompensation {
  id String @id @default(cuid())
  taskId String @unique
  model CompensationModel @default("FIXED_TOKEN")
  tokenAmount Decimal?
  hourlyRate Decimal?
  equityPercent Decimal?
  stableCoinAmount Decimal?
  vestingMonths Int?
  createdAt DateTime @default(now())
  task ProjectTask @relation(fields: [taskId], references: [id])
}

model TaskSubmission {
  id String @id @default(cuid())
  taskId String
  submittedById String
  content String @db.Text
  attachments String? @db.Text
  status SubmissionStatus @default("PENDING")
  reviewedById String?
  feedback String? @db.Text
  submittedAt DateTime @default(now())
  reviewedAt DateTime?
  task ProjectTask @relation(fields: [taskId], references: [id])
  submittedBy User @relation(fields: [submittedById], references: [id])
  reviewedBy User @relation(fields: [reviewedById], references: [id])
}

model Proposal {
  id String @id @default(cuid())
  projectId String
  creatorId String
  type ProposalType
  title String
  description String @db.Text
  options Json
  quorum Int
  threshold Int
  status ProposalStatus @default("DRAFT")
  votingStart DateTime?
  votingEnd DateTime?
  executionDelay Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  creator User @relation(fields: [creatorId], references: [id])
  votes Vote[]
  proposalExecutions ProposalExecution[]
}

model Vote {
  id String @id @default(cuid())
  proposalId String
  voterId String
  choice Int
  weight Decimal
  reason String? @db.Text
  txHash String?
  createdAt DateTime @default(now())
  proposal Proposal @relation(fields: [proposalId], references: [id])
  voter User @relation(fields: [voterId], references: [id])
}

model ProposalExecution {
  id String @id @default(cuid())
  proposalId String @unique
  executedAt DateTime @default(now())
  txHash String?
  result Json?
  proposal Proposal @relation(fields: [proposalId], references: [id])
}

model DelegatedVote {
  id String @id @default(cuid())
  projectId String
  delegatorId String
  delegateeId String
  tokenId String
  amount Decimal
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  expiresAt DateTime?
  revokedAt DateTime?
  project Project @relation(fields: [projectId], references: [id])
  delegator User @relation(fields: [delegatorId], references: [id])
  delegatee User @relation(fields: [delegateeId], references: [id])
  token ProjectToken @relation(fields: [tokenId], references: [id])
}

model Guild {
  id String @id @default(cuid())
  name String
  slug String @unique
  description String @db.Text
  website String?
  email String
  phone String?
  address String? @db.Text
  logo String?
  portfolio String? @db.Text
  specialties String? @db.Text
  isVerified Boolean @default(false)
  rating Decimal?
  reviewsCount Int @default(0)
  projectsCount Int @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ownerId String @unique
  projects Project[]
  owner User @relation(fields: [ownerId], references: [id])
  guildMembers GuildMember[]
  projectBids ProjectBid[]
  guildReviews GuildReview[]
}

model GuildMember {
  id String @id @default(cuid())
  guildId String
  userId String
  role String
  joinedAt DateTime @default(now())
  guild Guild @relation(fields: [guildId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model ProjectBid {
  id String @id @default(cuid())
  projectId String
  guildId String
  userId String
  proposal String @db.Text
  timeline String? @db.Text
  budget Decimal
  deliverables String? @db.Text
  status BidStatus @default("SUBMITTED")
  submittedAt DateTime @default(now())
  reviewedAt DateTime?
  project Project @relation(fields: [projectId], references: [id])
  guild Guild @relation(fields: [guildId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model GuildReview {
  id String @id @default(cuid())
  guildId String
  projectId String?
  userId String
  rating Int
  comment String? @db.Text
  createdAt DateTime @default(now())
  guild Guild @relation(fields: [guildId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model Shop {
  id String @id @default(cuid())
  name String
  description String? @db.Text
  address String? @db.Text
  phone String?
  email String?
  website String?
  logo String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ownerId String
  owner User @relation(fields: [ownerId], references: [id])
  products Product[]
  invoices Invoice[]
  sales Sale[]
  inventoryItems InventoryItem[]
  marketingCampaigns MarketingCampaign[]
  shopAnalytics ShopAnalytics[]
}

model Product {
  id String @id @default(cuid())
  shopId String
  name String
  description String? @db.Text
  sku String?
  price Decimal
  cost Decimal?
  category String?
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation(fields: [shopId], references: [id])
  user User @relation(fields: [userId], references: [id])
  saleItems SaleItem[]
  inventoryItems InventoryItem[]
}

model Invoice {
  id String @id @default(cuid())
  shopId String
  buyerId String
  invoiceNumber String @unique
  amount Decimal
  tax Decimal?
  discount Decimal?
  total Decimal
  status InvoiceStatus @default("DRAFT")
  dueDate DateTime
  paidAt DateTime?
  notes String? @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation(fields: [shopId], references: [id])
  buyer User @relation(fields: [buyerId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model Sale {
  id String @id @default(cuid())
  shopId String
  buyerId String?
  total Decimal
  tax Decimal?
  discount Decimal?
  paymentMethod PaymentMethod
  notes String? @db.Text
  createdAt DateTime @default(now())
  userId String
  shop Shop @relation(fields: [shopId], references: [id])
  buyer User @relation(fields: [buyerId], references: [id])
  user User @relation(fields: [userId], references: [id])
  saleItems SaleItem[]
}

model SaleItem {
  id String @id @default(cuid())
  saleId String
  productId String
  quantity Int
  price Decimal
  total Decimal
  sale Sale @relation(fields: [saleId], references: [id])
  product Product @relation(fields: [productId], references: [id])
}

model InventoryItem {
  id String @id @default(cuid())
  shopId String
  productId String @unique
  currentStock Int @default(0)
  minStock Int @default(0)
  maxStock Int?
  reorderPoint Int?
  lastRestocked DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation(fields: [shopId], references: [id])
  product Product @relation(fields: [productId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model MarketingCampaign {
  id String @id @default(cuid())
  shopId String
  name String
  description String? @db.Text
  platform String
  content String @db.Text
  mediaUrls String? @db.Text
  scheduledAt DateTime?
  status CampaignStatus @default("DRAFT")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userId String
  shop Shop @relation(fields: [shopId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model ShopAnalytics {
  id String @id @default(cuid())
  shopId String
  date DateTime
  revenue Decimal
  expenses Decimal
  profit Decimal
  salesCount Int
  newCustomers Int
  createdAt DateTime @default(now())
  shop Shop @relation(fields: [shopId], references: [id])
}

model ProjectToken {
  id String @id @default(cuid())
  projectId String @unique
  assetId String?
  name String
  symbol String
  totalSupply Decimal
  decimals Int @default(6)
  allocation Json
  vestingConfig Json?
  isDeployed Boolean @default(false)
  deployedAt DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  delegatedVotes DelegatedVote[]
  project Project @relation(fields: [projectId], references: [id])
  tokenHolders TokenHolder[]
  iCOS ICO[]
  taskEscrows TaskEscrow[]
  tokenSwaps TokenSwap[]
  tokenSwaps1 TokenSwap[]
  liquidityPools LiquidityPool[]
  liquidityPools1 LiquidityPool[]
}

model ProjectEquity {
  id String @id @default(cuid())
  projectId String
  userId String
  sharePercent Decimal
  investmentAmount Decimal
  grantedAt DateTime @default(now())
  project Project @relation(fields: [projectId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model Treasury {
  id String @id @default(cuid())
  projectId String @unique
  balance Decimal @default(0)
  tokenAssetId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project Project @relation(fields: [projectId], references: [id])
  treasuryTransactions TreasuryTransaction[]
}

model TreasuryTransaction {
  id String @id @default(cuid())
  treasuryId String
  type TransactionType
  amount Decimal
  description String?
  txHash String?
  proposalId String?
  createdAt DateTime @default(now())
  treasury Treasury @relation(fields: [treasuryId], references: [id])
}

model TokenHolder {
  id String @id @default(cuid())
  tokenId String
  userId String
  balance Decimal
  stakedAmount Decimal @default(0)
  lockedAmount Decimal @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  token ProjectToken @relation(fields: [tokenId], references: [id])
  user User @relation(fields: [userId], references: [id])
  tokenVestings TokenVesting[]
}

model TokenVesting {
  id String @id @default(cuid())
  holderId String
  totalAmount Decimal
  releasedAmount Decimal @default(0)
  startDate DateTime
  cliffEnd DateTime
  vestingEnd DateTime
  releaseFrequency VestingFrequency @default("MONTHLY")
  createdAt DateTime @default(now())
  holder TokenHolder @relation(fields: [holderId], references: [id])
}

model ICO {
  id String @id @default(cuid())
  tokenId String @unique
  fundingGoal Decimal
  currentFunding Decimal @default(0)
  minContribution Decimal?
  maxContribution Decimal?
  tokenPrice Decimal
  startDate DateTime
  endDate DateTime
  status ICOStatus @default("PENDING")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  token ProjectToken @relation(fields: [tokenId], references: [id])
  iCOContributions ICOContribution[]
}

model ICOContribution {
  id String @id @default(cuid())
  icoId String
  userId String
  amount Decimal
  tokenAmount Decimal
  paymentAsset String
  txHash String?
  status ContributionStatus @default("PENDING")
  createdAt DateTime @default(now())
  ico ICO @relation(fields: [icoId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model Wallet {
  id String @id @default(cuid())
  userId String
  address String @unique
  provider WalletProvider
  label String?
  isVerified Boolean @default(false)
  isPrimary Boolean @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user User @relation(fields: [userId], references: [id])
}

model TaskEscrow {
  id String @id @default(cuid())
  taskId String @unique
  funderId String
  tokenId String
  amount Decimal
  status EscrowStatus @default("FUNDED")
  txHashFund String?
  txHashRelease String?
  txHashRefund String?
  createdAt DateTime @default(now())
  fundedAt DateTime?
  releasedAt DateTime?
  refundedAt DateTime?
  task ProjectTask @relation(fields: [taskId], references: [id])
  funder User @relation(fields: [funderId], references: [id])
  token ProjectToken @relation(fields: [tokenId], references: [id])
}

model TokenSwap {
  id String @id @default(cuid())
  userId String
  fromTokenId String
  toTokenId String
  fromAmount Decimal
  toAmount Decimal
  exchangeRate Decimal
  fee Decimal @default(0)
  txHash String?
  status SwapStatus @default("PENDING")
  createdAt DateTime @default(now())
  completedAt DateTime?
  user User @relation(fields: [userId], references: [id])
  fromToken ProjectToken @relation(fields: [fromTokenId], references: [id])
  toToken ProjectToken @relation(fields: [toTokenId], references: [id])
}

model LiquidityPool {
  id String @id @default(cuid())
  token1Id String
  token2Id String
  reserve1 Decimal @default(0)
  reserve2 Decimal @default(0)
  totalShares Decimal @default(0)
  feePercent Decimal @default(0.003)
  isActive Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  token1 ProjectToken @relation(fields: [token1Id], references: [id])
  token2 ProjectToken @relation(fields: [token2Id], references: [id])
  liquidityProviders LiquidityProvider[]
}

model LiquidityProvider {
  id String @id @default(cuid())
  poolId String
  userId String
  shares Decimal
  token1In Decimal @default(0)
  token2In Decimal @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  pool LiquidityPool @relation(fields: [poolId], references: [id])
  user User @relation(fields: [userId], references: [id])
}

model ChatMessage {
  id String @id @default(cuid())
  userToId String
  userFromId String
  message String?
  status MessageStatus @default("NOTSEEN")
  ChatReaction Json?
  chatAttachmentId String?
  sentAt DateTime @default(now())
  seenAt DateTime?
  userTo User @relation(fields: [userToId], references: [id])
  userFrom User @relation(fields: [userFromId], references: [id])
  chatAttachment Attachment @relation(fields: [chatAttachmentId], references: [id])
}

model Attachment {
  id String @id @default(cuid())
  uploadedById String
  bucketPath String?
  type MimeType
  createdAt DateTime @default(now())
  lastUsedAt DateTime?
  chatMessages ChatMessage[]
  uploadedBy User @relation(fields: [uploadedById], references: [id])
}

